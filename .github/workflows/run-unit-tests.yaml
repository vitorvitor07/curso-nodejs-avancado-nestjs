name: CI - Unit Tests

on: [push]

jobs:
  run-unit-tests:
    name: Run unit tests
    runs-on: ubuntu-latest

    # definir passos necessários para baixar o app e rodar os testes

    steps:
      